# noticed desured functionality

# want to have custom function listed as python scripts

# listed in the folder if they want

# need to be able to chain orchestraction

# also noticed i want the function in the UI to remain persistent when i modify the running

# the pipeline does not remain
